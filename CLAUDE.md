# CLAUDE.md

このリポジトリの共通エージェントガイドは **AGENTS.md** にある。まずそれを読むこと。
(Codex CLI と Claude Code が同一のコンテキストを共有するため、
プロジェクト知識は AGENTS.md に一元化している。)

Claude Code 固有の補足:

- テスト実行: `python -m pytest tests/ -q`(全件パスを維持)
- 戦略・制約の根拠は `docs/strategy.md`、調査の一次情報は `docs/research/`
- 実弾発注コードの実装・有効化はユーザーの明示的指示があるまで行わない
- バックテスト結果を報告するときは、必ず対数尤度(結合 vs 市場単体)と
  検証条件(期間・分割・購入ルールの固定時点)を併記する
